{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "cargo_build",
      "type": "shell",
      "command": "~/.cargo/bin/cargo", // note: full path to the cargo
      "args": [
        "build"
        // "--release",
        // "--",
        // "arg1"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
